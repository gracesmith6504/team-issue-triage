apiVersion: v1
kind: Service
metadata:
  name: triage-dashboard
spec:
  type: ClusterIP
  selector:
    app: triage-dashboard
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
